import java.awt.*;
import java.applet.*;
public class labeldemo extends Applet{
	public void init(){
		Label a=new Label("one");
		Label b=new Label("two");
		Label c=new Label("three ");
		add(a);
		add(b);
		add(c);
	}
	
}